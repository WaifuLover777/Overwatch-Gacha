node_modules/
dist/
.wrangler/
.dev.vars
.env*.local
*.log
.DS_Store

# Only the static app is published. The API skeleton stays local until it has
# something in it; to include it, delete these two lines.
apps/api/
.github/workflows/deploy-api.yml

# Portraits and heroes.json ARE committed: the app serves them from its own origin.
